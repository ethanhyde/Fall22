#include <stdio.h>
#include <stdlib.h>

int main(int argc, char ** argv)
{

	for(int z = 0; z < argc; ++z)
	{
		for(int j = 0; j < v[i] != '\0'; ++i)
			{
			printf("%s\n", argv[z]);
			printf("%s\n", j);
			}
		}
		
		















return 0;

}
